<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".V_MainActivity">

    <TextView
    android:id="@+id/title_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="20dp"
    android:text="Record Breaker!"
    android:textSize="40sp" />

    <Button
        android:id="@+id/tutorial_button"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_text"
        android:layout_centerHorizontal="true"
        android:onClick="start0"
        android:text="Tutorial"
        android:textSize="40sp" />

    <Button
        android:id="@+id/level1_button"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/tutorial_button"
        android:layout_centerHorizontal="true"
        android:onClick="start1"
        android:text="@string/level_one"
        android:textSize="40sp" />

    <Button
        android:id="@+id/credits_button"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="40sp"
        android:text="credits"
        android:onClick="scoresActivity"
        android:layout_below="@id/level1_button"/>

</RelativeLayout>
